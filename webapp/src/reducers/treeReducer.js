// import * as types from '../actions/actionTypes';

const mockData = {
    grandparent: {
        
    }
};

const initialState = {

};

// reducer to manage tree
export default function treeReducer(state = initialState, action) {
    switch (action.type) {
        case 'test':
            return {
                
            };
        default:
            return state;
    }
};
